# Changelog

All notable changes are recorded here. The project follows Semantic Versioning after the `1.0.0` release.

## Unreleased

- Adopt the binding product and implementation specification at the repository root.
- Begin the canonical Rust `mcl` modular monolith.
- Reclassify the earlier Python finite-domain implementation as migration input rather than a finished product.
- Add immutable pinned Lean environments and canonical verifier artifacts.
- Add durable, idempotent Lean verifier requests with lease recovery and CLI inspection.
